<Window x:Class="slsk_batchdl.Gui.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:slsk_batchdl.Gui.ViewModels"
        d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings"
        Height="480" Width="520"
        MinHeight="400" MinWidth="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Soulseek Account -->
                <TextBlock Text="Soulseek Account"
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           Margin="0,0,0,16" />

                <TextBox
                    Text="{Binding SoulseekUsername, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="Username"
                    materialDesign:HintAssist.IsFloating="True"
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    Margin="0,0,0,12" />

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <PasswordBox
                        Grid.Column="0"
                        materialDesign:HintAssist.Hint="Password"
                        materialDesign:HintAssist.IsFloating="True"
                        materialDesign:PasswordBoxAssist.Password="{Binding SoulseekPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="0,0,8,0" />

                    <Button
                        Grid.Column="1"
                        Command="{Binding TestCredentialsCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Content="Test"
                        Height="40" />
                </Grid>

                <!-- Testing indicator -->
                <ProgressBar
                    IsIndeterminate="True"
                    Height="2"
                    Margin="0,0,0,8"
                    Visibility="{Binding IsTesting, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!-- Credential test result -->
                <TextBlock
                    Text="{Binding CredentialStatus}"
                    FontSize="12"
                    Margin="0,0,0,16">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Tomato" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CredentialStatus}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CredentialSuccess}" Value="True">
                                    <Setter Property="Foreground" Value="LimeGreen" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Download Defaults -->
                <TextBlock Text="Download Defaults"
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           Margin="0,0,0,16" />

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ComboBox
                        Grid.Column="0"
                        ItemsSource="{Binding AvailableFormats}"
                        SelectedItem="{Binding PreferredFormat}"
                        materialDesign:HintAssist.Hint="Format"
                        materialDesign:HintAssist.IsFloating="True"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}" />

                    <ComboBox
                        Grid.Column="2"
                        ItemsSource="{Binding AvailableBitrates}"
                        SelectedItem="{Binding PreferredBitrate}"
                        materialDesign:HintAssist.Hint="Min Bitrate (kbps)"
                        materialDesign:HintAssist.IsFloating="True"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Grid.Column="0"
                        Text="{Binding DownloadPath, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Download Path"
                        materialDesign:HintAssist.IsFloating="True"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        VerticalContentAlignment="Center"
                        Margin="0,0,8,0" />

                    <Button
                        Grid.Column="1"
                        Command="{Binding BrowseDownloadPathCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Padding="8" Height="40">
                        <materialDesign:PackIcon Kind="FolderOpen" />
                    </Button>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    IsCancel="True"
                    Margin="0,0,12,0"
                    Click="Cancel_Click" />
            <Button Content="Save"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
